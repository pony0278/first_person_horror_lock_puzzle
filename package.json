{
  "name": "first_person_horror_lock_puzzle",
  "private": true,
  "devDependencies": {
    "@types/three": "^0.160.0",
    "http-server": "^14.1.1",
    "playwright": "^1.62.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.0",
    "vite-plugin-singlefile": "^2.3.3",
    "vitest": "^4.1.10",
    "wait-on": "^9.1.0"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run deps && npm run test",
    "deps": "node tools/deps.mjs"
  }
}
