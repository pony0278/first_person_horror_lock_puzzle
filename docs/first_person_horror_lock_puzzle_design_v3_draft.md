# 第一人稱恐怖開鎖益智遊戲：設計文件 v3（草案）

> 文件狀態：**草案，討論中，未定案**
> 日期：2026-08-07
> 取代：`first_person_horror_lock_puzzle_design_v2.md`（定案後取代；v2 在此之前仍是開發依據）
> 目標平台：CrazyGames（首發，非獨佔）
> 技術：TypeScript + Three.js（上方）+ DOM／Canvas 2D（下方）
> 現況：F0 已凍結、驗收通過、已上線 https://pony0278.github.io/first_person_horror_lock_puzzle/

---

## 0. v2 → v3 主要變更

| 項目 | v2 | v3 |
| --- | --- | --- |
| 謎題結構 | **一種鎖**（撬鎖）＋規則變體（§7） | **三扇門三種謎題**：撬鎖 → 分流通電 → 推箱子 |
| 玩家不重新學習 | 同一介面貫穿全局 | 學習成本攤在前三局；三種介面跨局固定，之後每局都是熟面孔 |
| 提示系統 | 牆面提示是唯一資訊源，三層路徑 | **只有門 1 用牆面提示**；門 2 盤面自明、門 3 狀態全可見 |
| 側頭 | 玩家主動 45° 側頭 | **砍除**。奔跑經過提示牆時自動轉頭＋子彈時間（F0 已實作） |
| 環境 | 氛圍與升壓 | 升格為**資訊系統的一部分**：藏零件、燈閃當計時線索、倒影當模糊情報 |
| 取件互動 | 無 | 新增：一指按住回頭、第二指伸手把東西扯下來 |
| 環境衰變 | 單門內隨站位升壓 | **跨門不重置**：門 1 乾淨 → 門 2 滲液燈閃 → 門 3 水淹燈滅 |
| 撬鎖難度變體 | 滑落針／雙生針／多假針／六針（F1 實作） | **舞台取消**。型別（`PinRule`）保留於 `lock.ts`，不排入實作 |
| 逃脫成功 | 未定義 | 定義：門關、聲音全停、喘息 2~3 秒，直接下一局 |
| 跨局難度計數 | 以「局」計（隱性教學階梯） | 以**累計開門數**計，死亡重來不會讓門 1 變難 |

v3 的觸發點：玩家測試（F0 驗收）通過後,「三扇門都在讀牆上的順序」被判定為重複性風險；
且拍板**「動手把東西拼回去」的觸感是這個設計不可少的部分**。

---

## 1. 產品定位

### 一句話概念

> 怪物正在逼近，而你不知道還剩多少時間——而你缺的那一塊，就藏在黑暗裡。

### 核心體驗支柱（v3 修訂）

1. **隱藏的時限**：沒有倒數 UI，怪物距離是唯一的時間資訊。（不變）
2. **自願的恐懼**：回頭免費；但謎題會**誘導**你把視線伸進黑暗——什麼時候伸手是你的決定。
3. **三扇門三個問題**：規律與執行（撬鎖）、選路與風險（分流通電）、謹慎與代價（推箱子）。同一局不問你同一個問題兩次。
4. **上下分層資訊**：下方給操作，上方給事實。通電看門上的電弧，推錯看背後的重置鈕。
5. **短局重玩**：單局約 90 秒，死亡整局重來，重開 1.5 秒。

### 這款遊戲不是什麼

- 不是解謎合集——謎題是恐懼的載體，規模全部壓在「小心執行」而非「深度規劃」。
- 不是注意力管理遊戲——觀察仍然免費，成本永遠只有時間。
- 不是 Steam 產品（規模不符）。

---

## 2. 平台約束（承 v2，不變）

PEGI 12、初始下載 ≤ 20MB、16:9 響應式、最低 30 FPS、進場最多一次點擊、手機觸控優先。
已由 CI 守門：產物大小 5MB 警戒線（現況 643KB）、手機視窗自動化測試 158 項
（devicetest 57、safearea 16、interrupt 15、transit 64）。

---

## 3. 核心循環（v3 修訂）

```text
關卡開始
→ 奔跑約 3 秒（自動轉頭掃過提示牆＋子彈時間）
→ 門 1【圖形等差推理＋撬鎖】20 秒　→ 開門、關門
→ 奔跑（環境已惡化一級）
→ 門 2【分流管線】 20 秒　→ 開門、關門
→ 奔跑（環境再惡化）
→ 門 3【推箱子】18 秒　→ 開門 ＝ 逃脫成功
死亡（任何一扇）→ 切黑＋一句原因 → 1.5 秒 → 回到門 1
```

### 逃脫成功（v2 未定義，v3 定案）

門關上的瞬間，所有追逐聲**突然全停**，只剩喘息。停 2~3 秒，不開選單、不出結算畫面，
直接進下一局的奔跑。插頁廣告若出現，掛在這個位置（見 §15），絕不掛在死亡上。

### 時間預算

| 階段 | 時間 |
| --- | --- |
| 每段奔跑 | 3 秒 |
| 門 1 / 門 2 / 門 3 | 25 / 20 / 18 秒（隱藏） |
| 開關門演出 × 3 | 各 2~4 秒 |
| **單局總長** | **約 90 秒** |

死在門 3 損失約 60~70 秒的正確操作。這是設計上接受的上限（v2 §11 選 3 門的理由），
門與門之間的內容不得再加厚。

---

## 4. 輸入方案（v3 修訂）

### 上方（第一人稱視野）

| 手勢 | 效果 |
| --- | --- |
| 第一指按住 | 回頭 180°（怪物方向）。放開回彈。 |
| **第二指點／按住可互動物** | **取件／按鈕**：抓住它，往下拖每 48px 扯一下（零件落到面板），或按下背後的重置鈕 |
| 第一指按住＋向下拖曳 | 同上的單手退路：不瞄準，盲扯（要轉滿 130° 才算） |

- 不新增長按判定——回頭是恐慌動作，反應不能慢半拍。
- **為什麼是兩根手指**：轉過去之後，第一根手指必須留在螢幕上，否則視野立刻彈回正面。
  所以「看著它」與「碰它」在物理上不可能是同一根手指——一指方案下，玩家沒辦法
  一邊轉頭一邊點牆上的東西。第二根手指才是這個動作的自然形狀：
  一手固定住你不敢看的方向，另一手伸進去。
- 命中判定用**螢幕距離**（≥56px）而不是幾何 raycast：導管只有 3.6cm 粗，
  兩公尺外的幾何命中區比指尖還小。手會抖，準度不該是難度。
- 可互動物在視野中帶微弱的邊緣光；**轉到看得見它時，邊緣光轉成慢呼吸**——
  「可以伸手了」全靠光講，零文字（§8）。
- **沒有低頭視角**。零件只放兩種地方：身後（回頭可見）與牆上（自動轉頭帶到）。

### 下方（謎題面板，約 764 × 118~127 px 可用）

| 門 | 操作 |
| --- | --- |
| 門 1 撬鎖 | 橫向拖曳選針、上推頂針、下拉洩壓（F0 現制，不變） |
| 門 2 管線 | **點一下管格 → 旋轉 90°**。缺格處顯示空槽，零件取回後自動落入 |
| 門 3 推箱子 | **在箱子上往左/右滑 → 箱子滑行到撞停** |

撬鎖專用的 chrome 列（全部洩壓鈕＋歷程）只在門 1 顯示；門 2、門 3 把那 44px 還給盤面。

### 桌機

**左鍵按住回頭，右鍵點一下＝扯一下** —— 左手完全不用動：

```text
左鍵按住 → 轉過去 → 右鍵點 ×3（左鍵不放）→ 零件到手 → 放開左鍵彈回
```

右鍵不需要瞄準（畫面裡只有一個可互動物，準度不是難度），也永遠不會搶走視角。
曾經試過「左鍵交接給 S 鍵再用滑鼠點」的方案，三步驟太不流暢，實測後否決；
S 鍵佔視角＋滑鼠點擊仍保留為替代路徑，左鍵按住往下拖的盲扯也還在（同手機單手退路）。

---

## 5. 觀察與取件（v3 修訂）

**所有觀察仍然免費**——不損失進度，唯一成本是時間流逝（v2 §5 原則保留）。

新增的是**凝視的邊際成本**（F0 已有機制，v3 把它接上謎題）：

- 凝視走廊超過 `stareLimit`（約 1.4 秒），怪物會趁燈閃的瞬間前進一站。
- 暗處的零件**只在燈閃的 0.18 秒內可辨識**——找零件需要凝視，凝視餵養怪物，
  而燈閃既是你唯一看得見的時刻、也是牠前進的時刻。
- 牆上的導管要**拉 2~3 下**才扯得下來（現行 3 下），每一下都在消耗凝視時間。
  兩根手指同時佔著螢幕的期間，下方謎題面板是停用的（§5 的回頭失能）——
  取件的代價因此是雙重的：時間，加上這段時間裡你完全沒有在解謎。

這個耦合讓「被謎題要求的看」保有掙扎：不是要不要看，而是**還敢再看一次嗎**。

---

## 6. 計時器與怪物模型（承 v2，補充）

怪物誠實、禁止橡皮筋、三種接近曲線（等速／潛伏／壓迫）總時間守恆、
連續錯 3 次瞬移、極限成功窗口——全部不變。

補充：

- 三扇門各自有隱藏時限，開門的瞬間下一扇的計時尚未啟動，奔跑不計時。
  **時限的算法（v3 修訂，取代 v2 的 25/20/18）**：v2 三扇門是同一把鎖，砍秒數是唯一的難度軸；
  v3 三個謎題內在長度不同，一律遞減沒有依據。改成 `時限 ＝ 熟練玩家做完要多久 ＋ 逐門遞減的餘裕`：

  | 門 | 熟練時間 | 餘裕 | 時限 |
  | --- | --- | --- | --- |
  | 門 1 撬鎖 | ~8s | 12s | **20s**（F0 驗收實測值，程式現況） |
  | 門 2 管線 | 讀路 2.5 ＋配置 4 ＋取件 2.5 ＋送電 0.8 ≈ 10s | 12s | **20s** |
  | 門 3 推箱 | 待定 | 9s | 待定 |

  壓力照樣逐門升高（餘裕在縮），但不會因為門 2 事情比較多就被硬砍。

  門 2 的站位曲線固定為 **7／11／14／20 秒**：保留總時限與最後壓迫段，將第一次可見延後到 7 秒，讓玩家先讀盤、辨識缺件，再決定何時回頭取件。

### R.over 的一體兩用已拆開（實作註記）

`R.over` 以前同時表示「這局結束了」和「過場演出中，別跑回合機器」。門 2 要時鐘跑、
怪物動，就必須在互動階段放掉它。拆出三樣東西，並修掉三個**只有在計時器解凍後才會爆**的問題：

| 問題 | 症狀 | 拆法 |
| --- | --- | --- |
| `monster.visible` 用 `T.active` 當「運鏡中」 | `T.active` 在門 2 互動階段仍為 true → **怪物永遠隱形** | `cinematic()` 只涵蓋 transit 拉鏡頭的階段；「死亡演出」條件收斂成 `R.over && !R.won` |
| 衰變等級與站位索引共用 `ST.index` | 門 2 借 `ST.index=2` 推動衰變，站位卻在 22m → 怪物會**一次跳 20 公尺**（§6 禁止的橡皮筋） | `decay.floor` 管跨門累進的環境等級，`ST.index` 門 2 從 0 重新開始；`envLevel()` 取兩者較大值 |
| 正面事件 `eye` / `lever` | 門 2 沒有鑰匙孔也沒有拉把，兩者 `visible=false` → 事件觸發、消耗配額、**有聲音沒畫面** | 門 2 專屬池 `refl / badge / glitch`；badge＝有人從另一側刷卡，glitch＝LCD 抖動＋斷線迸火花 |

另外 `R.door`（哪一扇門）與 `R.limit`（這扇門的時限）取代直接讀 `CFG.round.limit`。

門 2 現已在抵達盤面時呼叫共用的回合初始化：牆鐘歸零、R.over 放行、站位從 22m 重新開始；
通電或死亡則以具名的 round 原因暫停牆鐘，下一扇門／重開時才解除。奔跑過場因此不計時，
背景與 WebGL context 遺失的既有暫停原因也能繼續疊加，不會互相誤放行。

門 1 與門 2 都使用站位比例 `[0.35, 0.20, 0.15, 0.30]`，對應 **7／11／14／20 秒**。門 1 先給掃過牆面兩端符號＋點數、辨認中央整格缺失並開始撬鎖的時間；門 2 先給讀盤與辨識缺件時間。兩種提示都不暫停牆鐘。

**刻意不做「假的綠燈」**：那很嚇人，但 LCD 是 §9 的事實層（reach 的抬頭讀數）。
怪物可以嚇你，門上的儀表不能騙你 —— 它一旦會說謊，上下同步的契約就毀了。
- 極限成功窗口三扇門都適用——門 3 的極限成功（怪物手伸進來的瞬間推完最後一箱）是整局的情緒頂點。
- 隱藏計時器在切背景與 WebGL context 遺失期間暫停（F0 已實作，具名暫停）。

---

## 7. 三扇門與謎題設計（v3 全面重寫）

### 設計原則

1. **一扇門一個問題**，同一局不重複問。
2. 規模全部壓在「小心執行」：任何一扇門的認知負荷都不得高到讓玩家忘記抬頭。
3. 介面跨局固定：門 2 永遠是管線、門 3 永遠是推箱子。學習成本只付一次。
4. 盤面每局從**驗證過可解**的池子抽（§11），純隨機禁止。

### 門 1【撬鎖】——符號＋點數缺格推理＋壓力執行

```text
鎖     4 針、1 假針；四個固定符號各自顯示 1～5 個點
牆     單排三格；每格符號在上、點數在下，中央整格被拔走
規則   兩端點數決定唯一等差中項；正向或反向，公差為 1 或 2
作答   在鎖面找出帶中項點數的符號，補成左→中→右後依序撬鎖
時限   20 秒；怪物站位切換 7／11／14／20 秒
鏡頭   奔跑只側掃 38°；抵達後回看正後方，題列留側邊、走廊在中央
```

- 2026-08-09 第一版「2 個算式例證＋4 組數字／圖像」因理解成本與滿版提示破壞恐怖構圖而否決。
- 第二版把牆縮成三個真針符號，恢復走廊氣氛，但牆直接給答案，失去推理。
- 第三版以獨立刮痕＋波浪狀箭頭表示 1→2；玩家先讀成波浪。第四版改為分叉，又要求玩家先猜「樹枝數代表數字」，都在學臨時圖像語言而非推理。
- 第五版採 1／2／抹除位／4 缺格序列，題型雖一眼可懂，難度卻落在「牆面數量 → 門面刻痕 → 撞針符號」的轉譯鏈，故否決。
- 第六版改成牆上四個同款撞針圖形＋1／3／5 等差刻點，移除唯一違規圖形。它消除了轉譯，但玩家無法一眼知道題目要求「排除一項」，教育成本仍高。
- 第七版用三角形／缺格／五邊形表示邊數 3→4→5。缺格語法成立，但只有兩個已知項，牆面沒有證據說明「邊數」才是變量；玩家仍在猜設計者的規則，因此否決。
- 第八版用同一條五點軌道的最左／缺格／最右表示空間中點。答案唯一且不需數邊，但候選看起來像滑桿介面，題型不像玩家熟悉的圖形缺格，也偏離「符號本身參與答案」的目標。
- 目前版本採**單排符號＋點數中央缺格**：左右兩端皆是固定撞針符號，下方直接呈現點數；中央整格同時隱藏符號與點數。玩家只需辨認 `左點數 → 缺格 → 右點數` 的等差關係，不需把牆面資訊翻譯成另一套門鎖語言。
- 已驗證的點數模板為 `1-2-3`、`2-3-4`、`3-4-5`、`1-3-5`，每局可正向或反向。假針取得真序列之外的唯一點數，因此中央中項只有一個候選。
- `src/logic/pin-puzzle.ts` 依 `trueOrder` 將模板三項綁到實際撞針；牆面只保存兩端的符號與點數，中央保存 `pin: null, count: null`，推演器必須從兩端與四個鎖面候選找出唯一中項。
- 牆面維持 1.35 × 0.52m、左牆 2.8m、回看 180°；中央剝落區涵蓋完整的符號＋點數欄位，以四個釘孔及殘片表達「整格被拔走」，後方走廊仍居中。
- `src/render/cutaway.js` 在四根撞針上方直接繪製與牆面同源的「固定符號＋動態點數」檢修牌；`src/render/doorclue.js` 維持移除。
- 撬鎖深度（鎖芯事實層、假針卡死、單支／全部洩壓）全部保留；玩家補回缺格後仍需記住方向並連續執行三步。
- `PinRule` 的機械變體（滑落針等）仍不排入本次實作，型別保留在 `src/logic/pins.ts` 備用。
### 門 2【管線通電】——取回、選路與承擔結果

```text
盤面   2 排 × 8 格；直管／彎管＋1～2 個機械分流器
目標   斷電配置安全路線，取回共用入口缺件，再按主斷路器測試
結果   正確＝解鎖；普通斷路＝退電；焦黑端點＝跳電切黑 1.2 秒
時限   20 秒；跳電期間牆鐘與怪物照常前進，不偷加秒、不直接判死
提示   空槽脈衝只說明缺件；分流刀閘、焦黑端點與電磁閂全部直接可見
```

- **缺件仍在身後變電室取回**：玩家可以先配置、後取件，或先取件、後配置；何時回頭仍由玩家決定。
- 缺件槽固定在第一個分流之前的共用入口。所有路線都需要它，因此取件仍是強制的，但空槽不會指出哪條支線安全。
- 配置階段完全斷電；不再用即時電流前緣逐格告訴玩家下一步。取回零件後，左側主斷路器以 ≥48px 觸控目標慢速脈動。
- 分流器是三個實體端口的機械刀閘，但一次只閉合 `W→E` 或 `W→S`。電流因此仍是一條單鏈，不會像 T 型管同時分成兩路。
- 按下主斷路器才進行整條路線測試：電流逐格掃過，正確路線抵達右下電磁閂；普通接錯停在斷點；碰到可見的焦黑端點則短路。
- 短路會讓專用跳電黑幕維持 1.2 秒，之後回到可編輯狀態。牆鐘與怪物照常運作，但不額外加罰秒，也不會第一次選錯就直接死亡。
- 三張已驗證盤面提供不同安全方向：第一刀往下、兩刀中的中路、以及保持上路。每張恰好一組分流狀態可開門，至少一條支線可配置成真正短路。
- `solve()` 驗唯一安全路線；`solveShort()` 驗任意打亂後仍能配置到焦黑端點。盤面池同時驗證缺件強制、點擊成本與短路可達，禁止純隨機。
- 電磁門事實層維持：鎖定＝紅色實心橫槓，解鎖＝綠色缺口框；只有測試電流真的抵達門鎖時才跳綠。
- 幾何仍維持 2×8：收走 chrome 列後方格 55～60px，三種手機橫向視窗都高於 48px 觸控門檻；不改成 3 排，避免壓縮走廊與觸控尺寸。
### 門 3【推箱子】——謹慎與代價

```text
盤面   2 排 × 8~10 格、2~3 個箱子、目標格
推法   滑動制：往左/右滑一下，箱子滑行到撞停
解長   6~10 步
時限   18 秒、曲線隨機
提示   不需要。狀態全部可見
```

- **推死的箱子立刻變暗**（卡死感）。玩家不需要 Sokoban 經驗也能立刻知道「這局面死了」——
  損失的是已花的時間與接下來的儀式，不是「不知情地陪葬」。
- **重置鈕在後方牆上**：推錯之後，你必須回頭（直視怪物的方向），再用第二根手指按下它。
  盤面重置，時間不退還。失誤的代價不是數值，是**被迫轉身的儀式**。
- 盤面池由**離線求解器**生成並驗證（見 §13）；執行期只抽不生成。
- 死因語彙（§12）：「你把箱子推死了」／「時間不夠」。

### 為什麼是這三種（而不是其他）

- 撬鎖＝連續的手感（F0 驗證過的核心）；分流管線＝斷電配置後提交結果；推箱子＝不可逆的抉擇。
  三者的**認知動作**不同，但都是淺思考＋重執行，不會把玩家鎖死在下方。
- Match-3 仍然否決：方形盤面塞不進 10:1 橫條，且配對思考會鎖死注意力（§19）。

---

## 8. 資訊系統（v3 修訂，取代 v2 的提示系統）

三扇門的資訊來源刻意分化：

| 門 | 資訊來源 | 玩家要做的事 |
| --- | --- | --- |
| 門 1 | 牆面兩端符號＋點數、中央整格被拔除（自動轉頭必見） | 算出中項點數，補回對應符號並依完整序列執行 |
| 門 2 | 盤面分流、焦黑端點＋環境中的共用入口缺件 | 讀路、取回、配置後送電 |
| 門 3 | 盤面全可見＋背後的重置鈕 | 想清楚再推 |

- v2 的三層資訊路徑（上策／中策／下策）與殘缺提示**取消**——它們是「全部門都讀牆」時代的產物。
- 環境的資訊職責（§10）：燈閃＝節奏線索；門 3 常駐的積水倒影＝模糊的「後面有東西」，
  看得出存在、看不出距離——它不取代回頭，它**製造回頭的動機**。
- 符號系統（顏色＋形狀、零文字）沿用於門 1 與所有可互動物的標示。

---

## 9. 回饋與上下同步（承 v2 精神，按門展開）

**下方給操作，上方給事實**：

| 門 | 下方（操作） | 上方（事實） |
| --- | --- | --- |
| 門 1 | 撞針高度、洩壓 | 鎖芯累進角度（假針不動） |
| 門 2 | 管格旋轉、空槽 | 門邊導管電弧逐段亮起 |
| 門 3 | 箱子滑行、變暗 | 門上機關隨到位箱數逐段解除；背後重置鈕的紅光 |

靜音檢查原則不變：每個關鍵狀態都有視覺對應，聲音只是加速器。

---

## 10. 恐怖情緒工程（承 v2，新增兩項）

升壓綁距離、暗角不連續、jump scare 一次都不做、剪影與清晰度分層——全部不變。

### 新增一：環境衰變跨門不重置

```text
門 1   乾淨走廊、照明正常
門 2   牆面滲液、燈開始閃、地面積水
門 3   水淹過腳踝、燈幾乎全滅、倒影常駐
```

世界在壞掉，不只是怪物在靠近。機制沿用 F0 的 `decay` 系統（滲液等級、`lampDim`、`farDim`），
只是把「回合重置」改為「跨門累進、跨局重置」。

### 新增二：重置儀式（門 3）

推錯的那一刻你就知道等下要轉身。轉身時牠可能已經近了。
懲罰不是扣分，是**你自己走向它的那兩秒**。這是謎題失敗自然長出的恐懼，優先級高於任何演出。

---

## 11. 局結構與難度（v3 修訂）

### 三扇門的配置池

| 門 | 池內容 | 抽取 |
| --- | --- | --- |
| 門 1 | 撬鎖組合＋四組已驗證的點數等差模板＋序列外假針 | 每局把正向或反向模板綁到實際 `trueOrder`；生成時驗唯一中項答案 |
| 門 2 | 已驗證分流盤面 ×N＋共用入口缺件＋安全／斷路／短路結果 | 每局抽一張；驗唯一安全路線與至少一條短路 |
| 門 3 | 已驗證可解的推箱盤面 ×N（依解長分級） | 每局抽一 |

純隨機禁止（v2 原則）；池子按門分級，避免門 1 抽到難盤、門 3 抽到白給。

### 跨局計數

任何跨局遞進以**累計成功開門數**計，不以嘗試次數計。死亡本身不加難；已成功見過的門也不因死亡回退教學進度。門 1 始終維持 4 針、1 假針與同一套撬鎖操作；每局把正向或反向的三項點數模板重新綁到實際撞針，序列外點數維持唯一假針。

### 時限是輔助軸

門 1／門 2 現況皆為 20 秒；難度主軸回到操作、記憶與環境壓力，不再靠門 1 增加規則層數。門 3 時限等垂直切片實測後再定。

---

## 12. 失敗與重試（承 v2，補死因語彙）

整局重來、1.5 秒自動重開、結算只講一個原因——不變。

死因語彙按門擴充：

```text
門 1   「你卡在假針太久」「回頭看太多次」「連續操作錯誤」「時間不夠」
門 2   「你太晚去拿零件」「時間不夠」
門 3   「你把箱子推死了」「時間不夠」
```

仍然一句話、不解釋、不給建議。

---

## 13. 技術架構（承 v2，補謎題模組）

分層維持現況（F0 重構完成）：`logic/`（strict TS、單元測試）→ `state/dom` → `render/` → `game/`。

新增的純邏輯模組（全部進 `src/logic/`，可測）：

```text
pipe.ts      管線盤面：格型別、旋轉、連通性判定、盤面池
sokoban.ts   推箱盤面：滑動推演算、死鎖判定、盤面池
solver/      離線求解器：生成 + 驗證推箱盤面（開發工具，不進產物）
```

- 求解器是門 3 開發成本的大頭，但它離線跑、有測試，風險可控。
- 三扇門共用 `HiddenTimer`、站位狀態機、衰變系統——這些 F0 都已模組化。
- 撬鎖的 `LockState` 不動；`PinRule` 型別保留備用。

---

## 14. 美術與資產（承 v2，不變）

極低多邊形、無貼圖、純光照驅動。新增資產：管格、電弧（程序化）、箱子、重置鈕。
全部程序化生成，預算不變（上方模型＋材質 ≤ 2MB，現況產物 637KB）。

---

## 15. 變現（承 v2，方向收斂）

- 激勵廣告綁非核心（v2 選項二）；**復活維持否決**。
- 插頁：**只掛在逃脫成功後的 2~3 秒喘息之後**，或每 3~4 次死亡——絕不是每次死亡。
- SDK 接入排在 F2（見 §16），gameplay 事件穩定後再接。

---

## 16. 開發階段（v3 修訂）

| 階段 | 內容 | 時程 |
| --- | --- | --- |
| ~~F0~~ | ~~單門撬鎖原型~~ **已完成**：驗收通過、已上線、CI/Pages 自動發布 | — |
| **F1** | 局結構（三扇門、門間奔跑、環境跨門衰變、逃脫成功）＋**門 2 管線**（含取件手勢、環境藏件、燈閃耦合）＋曲線隨機 | 3 週上限 |
| F2 | **門 3 推箱子**（求解器、盤面池、死鎖顯示、背後重置）＋極限成功全門＋能力拾取＋廣告 SDK | 4 週 |
| F3 | 盤面池擴充、更多環境事件、（評估）第四種謎題 | — |
| F4 | 每日挑戰、成就、難度模式、數據分析 | — |

**門 2 先做的理由**：管線規則簡單、不需要求解器，先驗證「取件手勢＋環境藏件」這條新骨幹；
門 3 疊了三個新東西（盤面生成、死鎖顯示、背後重置），等骨幹驗證過再上。

F1 時程仍是硬上限：三週到期，拿當下的版本測。

---

## 17. F1 規格與驗收

### 範圍

```text
局結構   三扇門、死亡整局重來、逃脫成功演出
門 1     F0 現制撬鎖（完整提示）
門 2     管線通電：2×8 旋轉盤面、缺 1 件、取件手勢、燈閃耦合
門 3     暫用佔位（撬鎖六針版），F2 換推箱子
環境     衰變跨門累進；門 2 起燈閃、滲液
排除     推箱子、能力拾取、廣告、每日挑戰
```

### 驗收標準（5~8 人，錄螢幕，玩三局，不問好不好玩）

| # | 問題 | 通過標準 | 權重 |
| --- | --- | --- | --- |
| 1 | 玩家能自行發現並取回缺件嗎 | 8 成在門 2 首次遭遇時 60 秒內完成取件（含死亡重試） | **必須通過** |
| 2 | 取件時會猶豫嗎 | 觀察：取件前有可辨識的停頓／回頭確認行為 | **必須通過** |
| 3 | 死亡時知道原因嗎 | 8 成能說出自己為什麼死（延續 F0 標準） | **必須通過** |
| 4 | 環境惡化被感知嗎 | 5 成能說出「愈後面的門環境愈糟」 | 可調整 |

**1 沒過**＝取件的可發現性不足，調邊緣光與空槽的指示強度，不是加教學 UI。
**2 沒過**＝v3 的核心賭注（強迫取物仍能保有猶豫）不成立——回到 v2 的資訊誘導版重議。

---

## 18. 設計原則（v3 修訂版）

1. 玩家一進來就在 gameplay 裡。
2. 怪物誠實，禁止橡皮筋。
3. 觀察免費；成本永遠是時間，包括凝視的邊際成本（燈閃耦合）。
4. 一扇門一個問題，同一局不重複問。
5. 謎題規模壓在小心執行，不在深度規劃——玩家必須還有餘裕害怕。
6. 下方給操作，上方給事實。
7. 難度主軸是謎題類型與環境，時間壓縮是輔助軸。
8. 恐懼來自玩家自己伸手進黑暗，不來自彈出式驚嚇。
9. 失誤的最重懲罰是儀式（轉身），不是數值。
10. 結算只講一個原因。
11. 盤面一律從驗證過的池子抽，純隨機禁止。
12. 先驗證核心再擴充；每期時程是硬上限。

---

## 19. 已否決與翻案紀錄

### v3 翻案（附代價）

| 項目 | v2 判定 | v3 判定 | 翻案理由 | 代價 |
| --- | --- | --- | --- | --- |
| 管線／線路類謎題 | 否決：方形盤面不適配寬扁操作區 | **翻案**：門 2 採用 | 電流左→右，橫條正是左右最寬裕的形狀；2×8 實測放得下 | 需新介面美術與教學成本 |
| 「一種鎖」原則（v2 §7） | 核心原則 | **作廢** | 三扇門讀同一面牆被玩家測試判定為重複風險；改為「三介面跨局固定」攤平學習成本 | 撬鎖變體舞台取消；文件與程式的 `PinRule` 閒置 |
| 純解謎門 | 否決：思考鎖死注意力 | **有條件翻案** | 規模壓在小心執行＋與環境耦合（取件、背後重置），謎題主動把視線推向上方 | 「自願的恐懼」部分讓位給「誘導的恐懼」，F1 驗收標準 2 專門驗這件事 |

### 門 2 岔路：用單路分流器翻案

先前的結構結論仍成立：只靠直管與彎管，2×N 的角到角簡單路徑全部單調往東，
普通管型本身會宣告轉向，做不出真正選擇。`tests/pipe.test.ts` 仍保留窮舉證明。

本版沒有加入會同時分電的 T 型管，而是加入**單路機械分流器**：

- 格子有西、東、南三個實體端口，但刀閘一次只接西→東或西→南。
- 玩家是在兩條路之間選一條，不是讓電流同時分岔；所以前緣火花仍唯一。
- 盤面斷電時先配置，按主斷路器才提交整條路線，消除「跟著亮點逐格修正」的答案洩漏。
- 焦黑短路端點在測試前可見；錯誤來自沒有先追完路線，不是隱藏陷阱。
- 短路只造成可恢復的 1.2 秒跳電。真正死亡仍來自怪物與 20 秒牆鐘，維持公平性。

這個翻案把門 2 的問題從「沿唯一路徑執行」改為「讀路→選路→送電→承擔結果」，
同時保住 2×8 手機觸控尺寸與單一電流前緣兩個既有地基。
### 維持否決

| 方向 | 理由 |
| --- | --- |
| Match-3／轉珠 | 方形盤面＋配對思考鎖死注意力，兩個問題都沒解 |
| 玩家可控側頭 | 自動轉頭＋子彈時間已覆蓋其資訊職責，省一個手機手勢 |
| 玩家操作的奔跑障礙 | 與 3 秒奔跑預算及手勢衝突（v2 原案）；演出型翻越可做 |
| 激勵廣告復活 | 摧毀死亡的重量（v2 原案） |
| 可見倒數 UI、橡皮筋、彈出式驚嚇、五扇門、能力選單 | v2 原案，理由不變 |

---

## 20. 未決項目

| 項目 | 說明 |
| --- | --- |
| 門 2 盤面池規模與管型組合 | 幾張盤、彎管比例，F1 調校 |
| 缺件邊緣光的強度 | 驗收標準 1 的主要調整旋鈕 |
| 門 3 盤面池規模與解長分佈 | 求解器參數，F2 調校 |
| 推死變暗的具體表現 | 變暗／碎裂／滲水，F2 選 |
| 三種接近曲線參數 | 承 v2 未決，F1 調校 |
| 逃脫成功 2~3 秒的演出內容 | 只有喘息？遠處新的聲音？F1 決 |
| 門 3 佔位（F1 期間）用什麼 | 暫定撬鎖六針版，或直接鎖住只測兩門 |

---

## 21. 最終核心（v3 修訂）

v2 證明了「不知道還剩多少時間，唯一的確認方式是回頭」有張力（F0 驗收通過）。
v3 要證明的是它的延伸：

> **當謎題缺了一塊，而那一塊就在黑暗裡——玩家會多快伸手？伸手的時候，他敢不敢看著牠？
> 推錯了一步，必須轉身面對牠才能重來——他會立刻轉身，還是先呆住半秒？**

只要取件前有猶豫、轉身前有停頓、門關上時有釋放感，v3 的方向就成立。
如果取件變成無感的例行公事（F1 驗收標準 2 不過），退回 v2 的資訊誘導版重議——
翻案紀錄（§19）就是為那一天準備的。
